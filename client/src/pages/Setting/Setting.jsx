import { useContext } from "react";
import { AuthContext } from "../../context/auth.context";
const Setting = () => {
    const { SwitchLang } = useContext(AuthContext);

    return (
<></>
    );
};
export default Setting;
